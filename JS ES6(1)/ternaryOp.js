var age = 18;
/*
if(age >= 18){
    type = 'adult';
}else{
    type = 'child';
}*/

var type = (age >= 18) ? 'adult' : 'child';
console.log(type);
var num =10;
var t = (num >= 1) ? 'positive' : (num == 0) ? 'Zero' : 'nagetive';
console.log(t);

var e= 5;
var acc = e>=5 ;// if output is true false can write in this way
console.log(acc);